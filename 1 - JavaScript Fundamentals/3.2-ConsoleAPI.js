/*
 * Log: is a function in JavaScript which is used to print any kind of variables defined before in it or to just print any message that needs to be displayed to the user.
 */

const myVar = true;

console.log(245);
console.log(`I am a text!`);
console.log(myVar);

/*
 * Error: the error() method writes an error message to the console.
 */

console.error(`Error message!`);

//  Read more: https://nodejs.org/api/console.html
