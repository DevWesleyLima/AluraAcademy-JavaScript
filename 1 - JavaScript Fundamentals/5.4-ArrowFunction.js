/*
 *  Arrow Function
 */

//  1st Example
function showUp(name)
{
    return `My name is ${name}!`;
}

//  1st Arrow Function
const showUpArrow = name => `My name is ${name}!`;
const sum = (value1, value2) => value1 + value2;

//  2nd Arrow Function
const sumSmallNumbers = (value1, value2) =>
{
    if(valu1 || value2 > 10)
    {
        return `Only numbers 1 to 9`
    }
    else
    {
        return value1 + value2;
    }
}
